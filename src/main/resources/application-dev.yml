spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/devsync
    username: devsync
    password: devsync123
    driver-class-name: org.postgresql.Driver

  redis:
    host: localhost
    port: 6379

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect

    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

        jdbc:
          time_zone: UTC

management:
  endpoints:
    web:
      exposure:
        include: mappings

jwt:
  secret: "DevSyncSuperSecretSecureSigningKey2026EnterpriseEdition"
